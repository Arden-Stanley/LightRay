#ifndef SCENE_H
#define SCENE_H

#include "Common.h"

namespace LR {
    /*class Scene {
        public:
            Scene();
            ~Scene();
            void AddObject(const Sphere &sphere);
            void Render() const;
        private:
            std::vector<Sphere> _objects;
    };*/
}

#endif